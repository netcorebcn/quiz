apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: quiz-cli
spec:
  template:
    metadata:
      labels:
        app: quiz-cli
    spec:
      containers:
      - name: quiz-cli
        image: ${REGISTRY}/quiz-cli:${TAG}